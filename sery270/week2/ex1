#include <stdio.h>

int main() {

	int a, b, c;
	int n = -1;

	scanf("%d %d %d", &a, &b, &c);
	
	if (c > b)
	{
		while (true)
		{
			n += 1;
			if (c*n > a + b*n) {
				break;
			}
			

		}
	} 

	printf("%d", n);

	return 0;
}
