#include <stdio.h>

int main() {

	int m, n;

	scanf("%d %d", &m, &n);

	//m이 1이나 2, 혹은 짝수일 경우를 고려함
	while (1) {
		switch (m)
		{
		case 1:
			m += 1;
			continue;
		case 2:
			printf("%d\n", m);
			m += 1;
			continue;
		default:
			if (m % 2 == 0) {
				m += 1;
				break;
			}
			else {
				break;
			}
		}
		break;
	}

	for (int i = m; i <= n; i += 2) { //2을 제외한 짝수는 소수가 될 수 없음, m이 2일땐 위에서 출력했음
		if (i == 3) {
			printf("%d\n", i);
			continue;
		}
		for (int j = 3; j <= i - 2; j += 2) {
			if (i % j == 0) {
				break;
			}
			if (j >= i - 2) {
				printf("%d\n", i);
			}

		}

	}


	return 0;
}
