ax=0 // 구할 x좌표
by=0
x=[] // 입력받을 x좌표들
y=[]

for i in range(1,4): // 3번 띄어쓰기를 구분자로 입력받음
    ax,by = input().split()
    x.append(ax)
    y.append(by)
    
if x[0]==x[1]: // 0,1이 같은 경우 2번째 x좌표
    print(x[2])
elif x[0]==x[2]: // 0,2가 같은 경우 1번째 x좌표
    print(x[1])
else:
    print(x[0]) // 아니라면 0번째 x좌표

if y[0]==y[1]:
    print(y[2])
elif y[0]==y[2]:
    print(y[1])
else:
    print(y[0])
